#pragma once

#include"command_processor.hpp"
#include"pg_connection.hpp"

namespace matching_engine{

// Восстановление состояния при старте (REQ-REC-01, REQ-REC-02, REQ-REC-05;
// проверяет на практике REQ-REC-03, REQ-REC-04, REQ-IDEM-05).
// Фиксированный порядок шагов, менять нельзя:
//   1. прочитать активные заявки, строго по возрастанию sequence_number;
//   2. поместить их в книгу минуя сопоставление (сделок при восстановлении
//      не возникает, даже если в книге уже есть встречные заявки с
//      пересекающимися ценами);
//   3. выставить SequenceGenerator в максимум по ВСЕЙ таблице orders + 1
//      (не только по активным — иначе следующая вставка столкнётся с
//      UNIQUE на sequence_number);
//   4. загрузить processed_commands и прогреть кеш идемпотентности.
// Только после этого движок готов принимать новые команды. Соединение не
// создаёт — принимает уже открытое Application; processor меняет только через его узкие методы
// restoreOrder()/warmCache(), доступа к его engine_/cache_ не получает.
// Пустая база — штатный случай: восстановит ноль заявок и ноль команд,
// счётчик номеров останется на начальном значении.
void recoverState(PgConnection& connection, CommandProcessor& processor);

}
