**# Matching Engine**

Упрощённый биржевой движок сопоставления заявок (Matching Engine). Программа принимает заявки на покупку и продажу финансового инструмента, хранит активные заявки в книге заявок (Order Book) и автоматически совершает сделки, когда цена покупателя пересекается с ценой продавца.

\---

**## Содержание**

\- [Принцип работы Limit Order Book]\(#принцип-работы-limit-order-book)
\- [Price-Time Priority]\(#price-time-priority)
\- [Архитектура]\(#архитектура)
\- [Структуры данных]\(#структуры-данных)
\- [Формат JSON-команд]\(#формат-json-команд)
\- [Сборка]\(#сборка)
\- [Запуск]\(#запуск)
\- [Запуск тестов]\(#запуск-тестов)
\- [Примеры работы]\(#примеры-работы)

\---

**## Принцип работы Limit Order Book**

**\*\*Limit Order\*\*** — заявка с явно указанной ценой. Покупатель указывает максимальную цену, которую готов заплатить; продавец — минимальную цену, за которую готов продать.

**\*\*Книга заявок (Order Book)\*\*** хранит все активные лимитные заявки, упорядоченные по цене и времени поступления. Она состоит из двух сторон:

\- **\*\*BUY (Bid)\*\*** — заявки на покупку, отсортированные по убыванию цены (лучшая — самая высокая цена)
\- **\*\*SELL (Ask)\*\*** — заявки на продажу, отсортированные по возрастанию цены (лучшая — самая низкая цена)

**\*\*Сделка\*\*** совершается, когда цена лучшей SELL-заявки не превышает цену лучшей BUY-заявки, то есть когда спред пересекается:

\`\`\`
BUY price >= SELL price → сделка возможна
\`\`\`

**\*\*Цена сделки\*\*** определяется ценой заявки, которая уже находилась в книге на момент поступления встречной заявки.

\---

**## Price-Time Priority**

Алгоритм сопоставления реализует приоритет **\*\*Price-Time\*\***:

**\*\*1. Price Priority\*\*** — в первую очередь исполняются заявки с лучшей ценой:

\- Для BUY: чем выше цена, тем выше приоритет
\- Для SELL: чем ниже цена, тем выше приоритет

\`\`\`
SELL в книге:   99 → 100 → 101   (приоритет слева направо)
BUY  в книге:  101 → 100 →  99   (приоритет слева направо)
\`\`\`

**\*\*2. Time Priority\*\*** — при одинаковой цене первой исполняется заявка, поступившая раньше (FIFO):

\`\`\`
Три BUY-заявки по цене 100, поступившие в порядке #10 → #20 → #30:
Приоритет: #10 > #20 > #30
\`\`\`

Одна входящая заявка может породить несколько сделок подряд — движок продолжает сопоставление, пока заявка не исполнена полностью или в книге не осталось подходящих встречных заявок.

\---

**## Архитектура**

\`\`\`
main()
  │
  └─► Application
        │
        ├─► CommandParser      парсинг JSON → объекты Command
        │
        ├─► MatchingEngine     бизнес-логика (не знает о JSON и stdout)
        │       │
        │       └─► OrderBook  хранение активных заявок
        │
        ├─► ReportPrinter      единственный класс, пишущий в stdout
        │
        └─► Logger             Singleton-обёртка над spdlog
\`\`\`

**### Описание классов**

\| Класс | Ответственность |
\|---|---|
\| \`Application\` | Читает \`argv\`, оркестрирует парсинг, выполнение команд и вывод результатов |
\| \`CommandParser\` | Валидирует JSON, проверяет поля и типы, создаёт объекты \`Command\` |
\| \`Command\` / \`AddCommand\` / \`CancelCommand\` / \`PrintCommand\` | Иерархия команд — передают данные от парсера к движку |
\| \`MatchingEngine\` | Принимает команды, запускает алгоритм сопоставления, хранит историю сделок |
\| \`OrderBook\` | Хранит активные заявки с поддержкой Price-Time Priority |
\| \`Order\` | Одна биржевая заявка; валидирует себя при создании |
\| \`Trade\` | Результат сделки: \`buyOrderId\`, \`sellOrderId\`, \`price\`, \`quantity\` |
\| \`ReportPrinter\` | Выводит сделки, состояние книги и ошибки |
\| \`Logger\` | Singleton Мейерса над spdlog; остальные компоненты не включают \`spdlog.h\` |

**\*\*Принцип разделения ответственности:\*\*** \`MatchingEngine\` не парсит JSON, не читает \`argc\`/\`argv\`, не пишет в консоль и не обращается к spdlog напрямую. \`ReportPrinter\` — единственный класс с \`std::cout\`.

\---

**## Структуры данных**

**### Хранилище заявок в OrderBook**

Для хранения заявок выбрана двухуровневая структура:

\`\`\`cpp
std::map\<int, std::deque\<std::shared\_ptr\<Order>>> buyLevels\_;
std::map\<int, std::deque\<std::shared\_ptr\<Order>>> sellLevels\_;
std::unordered\_map\<int, std::shared\_ptr\<Order>>   ordersById\_;
\`\`\`

**\*\*Почему \`std::map\<price, deque>\`?\*\***

\- \`std::map\` хранит ключи (цены) в отсортированном порядке и не требует \`std::sort\` после каждой вставки. Вставка и удаление — O(log N).
\- Для BUY лучшая цена — максимальная: итерируемся через \`rbegin()\`.
\- Для SELL лучшая цена — минимальная: итерируемся через \`begin()\`.
\- Пустые ценовые уровни удаляются из \`map\` автоматически.

**\*\*Почему \`std::deque\` внутри уровня цены?\*\***

\- Реализует FIFO: \`push\_back\` при добавлении, \`front\` для чтения лучшей заявки.
\- Поддерживает Time Priority без дополнительной сортировки.

**\*\*Почему \`std::unordered\_map\<id, Order>\`?\*\***

\- Поиск и удаление заявки по \`id\` за O(1) — необходимо для команды \`CANCEL\` и проверки дубликатов.

**\*\*Итоговые сложности:\*\***

\| Операция | Сложность |
\|---|---|
\| Добавить заявку | O(log N) |
\| Удалить заявку по id | O(log N) |
\| Найти заявку по id | O(1) |
\| Получить лучшую BUY / SELL | O(1) |
\| Итерация для PRINT | O(N) |

\---

**## Формат JSON-команд**

Программа принимает JSON-объект с массивом \`"commands"\` через аргумент командной строки.

**### ADD — добавить заявку**

\`\`\`json
{
    "type": "ADD",
    "id": 1,
    "side": "BUY",
    "price": 100,
    "quantity": 10,
    "command_id": "add-example-1"
}
\`\`\`

\| Поле | Тип | Описание |
\|---|---|---|
\| \`type\` | string | \`"ADD"\` |
\| \`id\` | int > 0 | Уникальный идентификатор заявки |
\| \`side\` | string | \`"BUY"\` или \`"SELL"\` |
\| \`price\` | int > 0 | Цена заявки |
\| \`quantity\` | int > 0 | Количество |
\| \`command_id\` | string | Уникальный идентификатор команды; обязателен для изменяющих команд (ADD, CANCEL, MODIFY) и обеспечивает идемпотентность — повтор с тем же \`command\_id\` не меняет книгу, не создаёт новых сделок и возвращает прошлый результат. \`PRINT\` этого поля не требует |

**### CANCEL — отменить заявку**

\`\`\`json
{
    "type": "CANCEL",
    "id": 1,
    "command_id": "cancel-example-1"
}
\`\`\`

**### PRINT — вывести книгу заявок**

\`\`\`json
{
    "type": "PRINT"
}
\`\`\`

**### Полный пример входных данных**

\`\`\`json
{
    "commands": [
        { "type": "ADD",    "id": 1, "side": "BUY",  "price": 100, "quantity": 10, "command_id": "full-example-add-1" },
        { "type": "ADD",    "id": 2, "side": "SELL",  "price": 105, "quantity": 5, "command_id": "full-example-add-2" },
        { "type": "ADD",    "id": 3, "side": "SELL",  "price": 99,  "quantity": 7, "command_id": "full-example-add-3" },
        { "type": "PRINT" },
        { "type": "CANCEL", "id": 2, "command_id": "full-example-cancel-1" },
        { "type": "PRINT" }
    ]
}
\`\`\`

\---

**## Сборка**

**\*\*Требования:\*\*** CMake ≥ 3.20, компилятор с поддержкой C++17 (GCC 13+ / Clang 16+), интернет для FetchContent.

Зависимости (\`nlohmann/json\`, \`GoogleTest\`, \`spdlog\`) загружаются автоматически при первой сборке.

\`\`\`bash
git clone \<repo-url>
cd matching\_engine

cmake -B build
cmake --build build
\`\`\`

После сборки в директории \`build\` появятся:
\- \`matching\_engine\` — исполняемый файл
\- \`matching\_engine\_tests\` — тестовый бинарь

\---

**## Запуск**

Для запуска нужен доступный сервер PostgreSQL: при старте приложение подключается к нему и само создаёт схему (таблицы и индексы) из файлов каталога \`database/\`, вручную создавать таблицы через \`psql\` не нужно. Параметры подключения (хост, порт, имя базы, пользователь) берутся из \`config/database.json\` (образец без пароля — \`config/database.example.json\`), путь переопределяется ключом \`--config\`. Пароль передаётся только через переменную окружения \`MATCHING\_ENGINE\_DB\_PASSWORD\` и в файлы конфигурации не попадает.

\`\`\`bash
./build/matching\_engine '\<json>'
\`\`\`

JSON передаётся одним аргументом командной строки. Рекомендуется обернуть в одинарные кавычки, чтобы экранировать от shell.

**\*\*Пример:\*\***

\`\`\`bash
./build/matching\_engine '{
    "commands": [
        { "type": "ADD",  "id": 1, "side": "BUY",  "price": 100, "quantity": 10, "command_id": "run-example-add-1" },
        { "type": "ADD",  "id": 2, "side": "SELL",  "price": 99,  "quantity": 7, "command_id": "run-example-add-2" },
        { "type": "PRINT" }
    ]
}'
\`\`\`

\---

**## Запуск тестов**

\`\`\`bash
ctest --test-dir build
\`\`\`

Или напрямую через бинарь для подробного вывода:

\`\`\`bash
./build/matching\_engine\_tests
\`\`\`

Покрытие тестами:

\| Модуль | Тестов |
\|---|---|
\| \`Order\` | 14 |
\| \`CommandParser\` | 10 |
\| \`OrderBook\` | 16 |
\| \`MatchingEngine\` | 16 |
\| **\*\*Итого\*\*** | **\*\*57\*\*** |

\---

**## Примеры работы**

**### Сделки нет — цены не пересекаются**

\`\`\`bash
./build/matching\_engine '{
    "commands": [
        { "type": "ADD",  "id": 1, "side": "BUY",  "price": 100, "quantity": 10, "command_id": "no-trade-add-1" },
        { "type": "ADD",  "id": 2, "side": "SELL",  "price": 105, "quantity": 5, "command_id": "no-trade-add-2" },
        { "type": "PRINT" }
    ]
}'
\`\`\`

\`\`\`
ORDER BOOK

SELL
105 5

BUY
100 10
\`\`\`

\---

**### Полное исполнение**

Обе заявки одинакового объёма — обе полностью исполняются и удаляются из книги.

\`\`\`bash
./build/matching\_engine '{
    "commands": [
        { "type": "ADD",  "id": 1, "side": "BUY",  "price": 100, "quantity": 10, "command_id": "full-fill-add-1" },
        { "type": "ADD",  "id": 2, "side": "SELL",  "price": 100, "quantity": 10, "command_id": "full-fill-add-2" },
        { "type": "PRINT" }
    ]
}'
\`\`\`

\`\`\`
TRADE buy=1 sell=2 price=100 quantity=10

ORDER BOOK

SELL

BUY
\`\`\`

\---

**### Частичное исполнение**

BUY на 10, SELL на 4 — BUY остаётся в книге с остатком 6.

\`\`\`bash
./build/matching\_engine '{
    "commands": [
        { "type": "ADD",  "id": 1, "side": "BUY",  "price": 100, "quantity": 10, "command_id": "partial-fill-add-1" },
        { "type": "ADD",  "id": 2, "side": "SELL",  "price": 100, "quantity": 4, "command_id": "partial-fill-add-2" },
        { "type": "PRINT" }
    ]
}'
\`\`\`

\`\`\`
TRADE buy=1 sell=2 price=100 quantity=4

ORDER BOOK

SELL

BUY
100 6
\`\`\`

\---

**### Исполнение одной заявки против нескольких (Price Priority)**

В книге три SELL-заявки по разным ценам. BUY выбирает наилучшие — сначала по цене 99, затем по 100.

\`\`\`bash
./build/matching\_engine '{
    "commands": [
        { "type": "ADD",  "id": 1, "side": "SELL",  "price": 101, "quantity": 5, "command_id": "priority-add-1" },
        { "type": "ADD",  "id": 2, "side": "SELL",  "price": 99,  "quantity": 3, "command_id": "priority-add-2" },
        { "type": "ADD",  "id": 3, "side": "SELL",  "price": 100, "quantity": 4, "command_id": "priority-add-3" },
        { "type": "ADD",  "id": 4, "side": "BUY",   "price": 100, "quantity": 7, "command_id": "priority-add-4" },
        { "type": "PRINT" }
    ]
}'
\`\`\`

\`\`\`
TRADE buy=4 sell=2 price=99 quantity=3
TRADE buy=4 sell=3 price=100 quantity=4

ORDER BOOK

SELL
101 5

BUY
\`\`\`

\---

**### Отмена заявки**

\`\`\`bash
./build/matching\_engine '{
    "commands": [
        { "type": "ADD",    "id": 1, "side": "SELL",  "price": 100, "quantity": 10, "command_id": "cancel-demo-add-1" },
        { "type": "CANCEL", "id": 1, "command_id": "cancel-demo-cancel-1" },
        { "type": "ADD",    "id": 2, "side": "BUY",   "price": 100, "quantity": 10, "command_id": "cancel-demo-add-2" },
        { "type": "PRINT" }
    ]
}'
\`\`\`

\`\`\`
ORDER BOOK

SELL

BUY
100 10
\`\`\`

Сделки нет — SELL была отменена до прихода BUY.

\---

**### MARKET Order

Для MARKET-заявок необходимо проверить:

- исполнение по лучшей доступной цене;
- исполнение одной MARKET-заявки против нескольких ценовых уровней;
- частичное исполнение при недостатке ликвидности;
- отсутствие MARKET-заявки в Order Book после обработки.

### MODIFY

Для `MODIFY` необходимо проверить:

- изменение цены;
- изменение количества;
- повторное прохождение изменённой заявки через Matching Engine;
- потерю временного приоритета при изменении цены;
- ошибку при попытке изменить несуществующую или уже исполненную заявку.

---

### Обработка ошибочной команды**

Ошибочная команда не прерывает обработку; остальные команды выполняются в штатном режиме.

\`\`\`bash
./build/matching\_engine '{
    "commands": [
        { "type": "ADD",    "id": 1, "side": "BUY",  "price": 100, "quantity": 10, "command_id": "error-demo-add-1" },
        { "type": "CANCEL", "id": 99, "command_id": "error-demo-cancel-1" },
        { "type": "PRINT" }
    ]
}'
\`\`\`

\`\`\`
ERROR: Cannot cancel: order with id 99 not found

ORDER BOOK

SELL

BUY
100 10
\`\`\`

\---

**### Логирование**

Все события записываются в stdout через spdlog. Пример вывода при выполнении сделки:

\`\`\`
[18:12:09] [info] Application started
[18:12:09] [info] Order received: id=1 side=SELL price=100 qty=10
[18:12:09] [info] Order added to book: id=1
[18:12:09] [info] Order received: id=2 side=BUY price=100 qty=10
[18:12:09] [info] Trade executed: buy=2 sell=1 price=100 qty=10
[18:12:09] [info] Order completely filled: id=1
[18:12:09] [info] Order completely filled: id=2
[18:12:09] [info] Order completely filled on arrival: id=2
[18:12:09] [info] Application finished
\`\`\`

Логируемые события: \`Application started/finished\`, \`Order received\`, \`Order added to book\`, \`Trade executed\`, \`Order partially/completely filled\`, \`Order cancelled\`, а также все ошибки с уровнем \`[error]\`.
---

## Дополнительный функционал

### Market Order

Поддерживается рыночная заявка через отдельный `MarketCommand`. MARKET-заявка не содержит цены, исполняется по лучшим доступным ценам противоположной стороны и не сохраняется в Order Book после обработки.

### MODIFY

Поддерживается команда `MODIFY` через отдельный `ModifyCommand`. Команда изменяет цену и количество существующей активной заявки. После изменения заявка повторно проходит через Matching Engine; при изменении цены она теряет прежний временной приоритет.

---

## Производительность и профилирование

Раздел закрывает REQ-PERF-01, REQ-PERF-03, REQ-PERF-04 и REQ-OPT-01. Все числа ниже получены реальными запусками на нагрузке из 100 000 команд (`workload.jsonl`, зерно генератора 42), а не оценены на глаз.

Сборка на момент замера — `RelWithDebInfo` (`-O2 -g`, символы не стрипнуты). До этой задачи проект собирался как `Release` (`-O3 -DNDEBUG`, без отладочной информации), и ради профилирования конфигурация была изменена: без символов в отчёте были бы адреса вместо имён функций. После переконфигурации весь набор тестов прогнан заново и проходит в новой конфигурации. Проект оставлен собранным как `RelWithDebInfo` — задаче о prepared statements и пакетной записи предстоит сравнивать замеры «до и после», а сравнимы они только в одной и той же конфигурации.

### Проверка памяти: Valgrind и цель сборки `memcheck`

Приложение проверено на штатном сценарии, задействующем подключение к БД, применение схемы, транзакционную запись заявок и сделки и штатное завершение:

```
valgrind --leak-check=full --show-leak-kinds=all ./build/matching_engine '<json с двумя встречными заявками и PRINT>'
```

| Величина | Значение |
|---|---|
| `definitely lost` | 0 байт в 0 блоках |
| `indirectly lost` | 0 байт в 0 блоках |
| `possibly lost` | 0 байт в 0 блоках |
| `still reachable` | 262 байта в 6 блоках |
| `ERROR SUMMARY` | 0 ошибок |

Утечек, созданных кодом проекта, нет. Оставшиеся 262 байта в шести блоках принадлежат не проекту: все шесть происходят из одной цепочки `PQsetdbLogin → PQconnectPoll`, то есть из однократной инициализации GSSAPI/Kerberos и таблиц методов OpenSSL внутри libpq при первом рукопожатии с сервером. Это статические кеши библиотек, живущие до конца процесса по их собственной конструкции. Что они не накапливаются, проверено сравнением: набор из шести блоков одинаков и на сценарии из трёх команд, и на прогоне 200 команд в режиме `--replay`, и на полном прогоне 189 тестов — то есть он не зависит ни от числа запросов, ни от числа транзакций.

Ресурсы самого проекта освобождаются деструкторами: `PGconn` и `PGresult` живут под `std::unique_ptr` с собственными удалителями (`PQfinish` и `PQclear`), незакоммиченную транзакцию откатывает деструктор `PgTransaction`.

Весь набор тестов запускается под Valgrind одной командой:

```
cmake --build build --target memcheck
```

Цель вызывает `valgrind` напрямую на тестовом бинарнике с `--error-exitcode=1`, а не через `ctest -T memcheck`: последний не связывает найденные дефекты с кодом возврата, и такая цель была бы зелёной всегда. На исправном коде цель завершается кодом 0. Её работоспособность проверена внесением заведомой утечки: цель обнаружила 27 648 байт в 108 блоках, `ERROR SUMMARY: 103 errors`, и сборка завершилась ненулевым кодом; после снятия утечки цель снова зелёная.

### Как приглушены логи и подготовлена база

Перед каждым прогоном таблицы `orders`, `trades`, `processed_commands` очищались:

```
psql -h <host> -p <port> -U <user> -d <database> -c "TRUNCATE orders, trades, processed_commands"
```

Хост, порт, имя базы и пользователя подставляются из вашей конфигурации (`config/database.json`, образец — `config/database.example.json`); в этом файле они не приводятся намеренно.

Это обязательно: `workload.jsonl` каждый раз несёт одни и те же `command_id`, и непустая база после предыдущего прогона превращает измерение в замер идемпотентного кеша, а не движка.

Логи на уровне `info` (~2,8 строки на команду в обычном режиме) приглушались штатной переменной spdlog:

```
SPDLOG_LEVEL=off
```

своих ключей для этого не заводилось.

### Способ снятия профиля (REQ-PERF-03, критерий 6)

`kernel.perf_event_paranoid = 2` — аппаратные счётчики (`cycles`, `instructions` и т. п.) в этом окружении (WSL2) недоступны: `perf record` с ними не программируется. Профиль снят программным событием `cpu-clock`, и из-за того же `paranoid = 2` охват — только пользовательское пространство (в отчёте это видно по имени события `cpu-clock:u`, суффикс `:u`). Время, проведённое в ядре (системные вызовы, в том числе сетевой ввод-вывод с PostgreSQL), в такой профиль не попадает в принципе — это ограничение метода, а не находка о коде.

```
perf record -e cpu-clock -g -o perf.data -- ./build/matching_engine --replay workload.jsonl
perf report -i perf.data --stdio --sort=overhead,symbol -g none
```

Профиль собран на 100 000 команд, 61 626 отсчётов события `cpu-clock:u`.

### Общее время прогона — где эти 100 000 команд проводят время на самом деле

CPU-профиль ниже отвечает только на вопрос «на что уходит небольшой процессорный кусок». Прежде чем его смотреть, важнее увидеть, что этот кусок — действительно небольшой. Замер обёрткой `/usr/bin/time -v` вокруг того же прогона:

```
SPDLOG_LEVEL=off /usr/bin/time -v ./build/matching_engine --replay workload.jsonl
```

Сырой результат (100 000 команд, 33 447 сделок, 0 дублей, 0 пропусков):

| Величина | Значение |
|---|---|
| Общее время (wall clock) | 10:38.01 = 638,01 с |
| Время в пользовательском режиме (user) | 36,49 с |
| Время в ядре (sys) | 49,09 с |
| Заявленный `perf`/`time` процент CPU | 13% |
| Ожидание (wall − user − sys) | 552,43 с (86,6% от wall) |
| Внутренний таймер приложения (только цикл `runReplay`, без подключения/схемы/восстановления) | 602 512 мс = 602,51 с |

86,6% времени прогона процесс не выполняет код ни в пользовательском, ни в ядерном режиме — он заблокирован на чтении из сокета, ожидая ответа PostgreSQL. Это согласуется с 468 376 добровольными переключениями контекста за тот же прогон (`Voluntary context switches` из того же `time -v`): на команду с реальным изменением состояния уходит не один, а несколько раунд-трипов к БД — `BEGIN`, вставка/обновление заявки, вставка сделки (если она была), вставка `processed_commands`, `COMMIT` — и почти на каждом процесс уходит в ожидание.

**Это и есть новая базовая линия для задачи 12** — снятая в текущей конфигурации сборки (`RelWithDebInfo`, `-O2`). Число 388 секунд из задачи 10 получено в другой конфигурации (`Release`, `-O3`) и напрямую с этим числом не сравнимо.

Отдельный прогон под `perf record` (тот же файл нагрузки, база тоже была предварительно очищена) показал внутренний таймер приложения 376 808 мс — заметно меньше, чем в прогоне без `perf`. Это не ошибка и не более быстрая конфигурация: два прогона к PostgreSQL по сети закономерно колеблются по времени ожидания (тёплый кеш страниц ОС и буферный кеш PostgreSQL после первого прогона, общая загруженность WSL2-хоста). Оба числа получены реальными запусками; для параметра «сколько всего занимает прогон» ориентир — прогон с `/usr/bin/time -v` (в нём же есть разбивка user/sys), а не более быстрый прогон под `perf`.

### Распределение процессорного времени (REQ-PERF-03, критерий вопроса 1)

Топ-15 функций по `Self` из `perf report --stdio --sort=overhead,symbol -g none` (имена функций разрешены — символы в бинарнике не стрипнуты):

| % Self | Функция |
|---|---|
| 4,42% | `malloc` |
| 3,14% | `__memmove_evex_unaligned_erms` |
| 1,86% | `cfree` (glibc) |
| 1,47% | `__strcmp_evex` |
| 1,40% | `std::_Hashtable<..., ExecutionResult>::_M_find_before_node` — поиск в кеше идемпотентности по `command_id` |
| 1,38% | `__errno_location` |
| 1,35% | `matching_engine::OrderRepository::save` |
| 1,23% | `EVP_CIPHER_CTX_get_iv_length` (OpenSSL) |
| 1,17% | `ERR_clear_error` (OpenSSL) |
| 1,07% | `__memcmp_evex_movbe` |
| 1,01% | `BIO_ctrl` (OpenSSL) |
| 0,93% | `__poll` |
| 0,93% | `matching_engine::OrderBook::removeFromLevels` |
| 0,91% | `_int_free` |
| 0,90% | `PQgetResult` (libpq) |

Для целостной картины функции сгруппированы по темам (группировка ручная, по подстроке в имени символа из полного `perf report`, не встроенная возможность `perf`; группы местами пересекаются по смыслу, а не строго дизъюнктны):

| Группа | % от `cpu-clock:u` |
|---|---|
| Примитивы libc: `malloc`/`free`/`new`/`delete`, `memmove`/`memcmp`/`strcmp`/`strlen`, `basic_string` | ≈33,4% |
| OpenSSL/TLS (`EVP_*`, `BIO_*`, `SSL_*`, `CRYPTO_*`, `ERR_*`, `OSSL_*`) | ≈12,8% |
| Свой код проекта (`matching_engine::*`) — весь | ≈11,4% |
| — из них: собственно сопоставление (`OrderBook::*`, `MatchingEngine::*`, `Order::fill/cancel`) | ≈2,9% |
| — из них: обвязка над БД (`*Repository::save/insert`, `PgConnection/PgResult/PgTransaction`, `PersistenceService::save`) | ≈3,7% |
| — из них: кеш идемпотентности (`CommandCache::put/find` + сама хеш-таблица) | ≈1,5% |
| — из них: разбор и диспетчеризация команд (`CommandParser`, `CommandProcessor`, `Application::processCommand/runReplay`) | ≈1,7% |
| `nlohmann::json` (разбор входного JSON и сборка результата для БД) | ≈8,0% |
| `libpq` — символы драйвера, включая его внутренние буферы `PQExpBuffer` | ≈3,5% |
| Системная обвязка (`__poll`, `__libc_recv`, `__send`, `__errno_location`, `pthread_once`, локаль) | ≈5,3% |
| Прочее / неклассифицированное (в том числе нерасшифрованные адреса без символов в сторонних `.so`, длинный хвост из тысяч записей менее 0,05% каждая) | остаток до 100% |

Сумма всех строк полного отчёта (24 600 строк, включая записи с округлением до 0,00%) — 96,52% от общего числа отсчётов; недостающие ~3,5% — тот самый длинный хвост.

### Ответы на четыре вопроса ТЗ

**1. Какие функции занимают больше всего процессорного времени.** `malloc` (4,42%) и связанные с ним примитивы работы с памятью и строками — `__memmove_evex_unaligned_erms`, `cfree`, `_int_free`, `__strcmp_evex`, `__memcmp_evex_movbe` — вместе это самая тяжёлая группа (≈33% CPU-профиля). Дальше идёт поиск в кеше идемпотентности (1,40%) и код одного репозитория — `OrderRepository::save` (1,35%). Это не адреса, а разрешённые имена: `RelWithDebInfo` дал полную символизацию.

**2. Сколько занимает собственно сопоставление.** Функции `OrderBook`/`MatchingEngine`/`Order::fill`/`Order::cancel` вместе — около 2,9% CPU-профиля. В пересчёте на пользовательское время прогона (36,49 с из `time -v`) это ≈1,06 с суммарно на все 100 000 команд — то есть **на два порядка меньше**, чем время, потраченное на ожидание БД. Сопоставление в этой системе не является узким местом ни разу.

**3. Сколько — работа с PostgreSQL.** Здесь два разных числа, и путать их нельзя. В CPU-профиле (программное событие, только пользовательское время) на путь к БД уходит: собственный код репозиториев и обёрток над `libpq` (≈3,7%) + сам `libpq` (≈3,5%) + шифрование TLS-соединения (≈12,8%) — итого ≈20% CPU-профиля, то есть даже в пределах маленького процессорного бюджета работа с БД тяжелее, чем сопоставление, примерно в 7 раз. Но это не главный ответ: реальная стоимость работы с БД — это 86,6% *всего времени прогона* (552,43 с из 638,01 с), проведённые в ожидании ответа PostgreSQL по сокету, — время, которое CPU-профиль в принципе не видит, потому что процесс в этот момент не выполняется ни на одном ядре.

**4. Есть ли неожиданные горячие места.**
   - **TLS-шифрование соединения с БД — ≈12,8% CPU-профиля**, хотя в коде проекта (`pg_connection.cpp`) SSL нигде явно не запрашивается. `PQsetdbLogin` не получает `sslmode`, поэтому используется значение по умолчанию `prefer`; локальный сервер PostgreSQL в этом окружении держит `ssl = on` (`SHOW ssl` → `on`, `pg_stat_ssl.ssl` → `t` даже для обычного `psql`-подключения), поэтому каждый раунд-трип к БД в приложении реально шифруется. Это не ошибка кода, а побочный эффект настройки локального сервера — но именно тот случай, когда профиль показывает то, что не видно по исходникам.
   - **Приглушённые логи не бесплатны.** `SPDLOG_LEVEL=off` подавляет вывод, но не построение сообщения: `Logger::info` (`src/logger.cpp`) принимает уже готовую `std::string`, а строки вроде `"Trade executed: buy=" + std::to_string(buyId) + ...` в `matching_engine.cpp` (15 мест только в этом файле) собираются оператором `+` и `std::to_string` заранее, до того как `spdlog` успевает проверить уровень и отбросить сообщение. Отдельно измерить долю именно этого в 33%-й группе аллокаций/строк не удалось (в профиле функция вызова не отличима от остальных источников временных строк — параметров SQL-запросов и сериализации JSON), но структурно это реальный источник лишней работы, который не исчезает от приглушения уровня лога.
   - Идемпотентный кеш (поиск по `command_id` в `std::unordered_map`) — 1,40% CPU-профиля сам по себе не аномалия (это ожидаемая цена идемпотентности), но заметно выше, чем всё сопоставление целиком, — стоит держать в уме при задаче 12.

### Что дальше

Задача 12 (prepared statements, пакетная запись) должна целиться не в CPU-профиль — там оптимизировать почти нечего, сопоставление и так дёшево, — а в 86,6% времени ожидания и в число раунд-трипов на команду. Здесь профилирование не даёт готового решения, только подтверждает, где искать: каждая команда — это несколько последовательных `PQexecParams` в одной транзакции, и именно их количество (а не их внутренняя стоимость) определяет итоговые 6 мс на команду.
